<UserControl x:Class="Labb3_NET22.PlayQuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_NET22"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="700">
    <UserControl.Resources>
        <local:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Quiz.Title}" FontSize="26" FontWeight="Bold" Foreground="#2c3e50"
                       HorizontalAlignment="Center" Grid.Column="0" />
            <!--Bulid Quiz-->
            <Button Grid.Column="1" Content="Build Quiz" Click="BuildQuiz_Click" Width="150" Height="40"
                    Background="#5C6BC0" Foreground="White" BorderThickness="0" 
                    HorizontalAlignment="Right"  />
        </Grid>
        <Grid Grid.Row="1" Margin="0,10,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding CurrentQuestion.Statement}" 
               FontSize="18" TextWrapping="Wrap" Grid.Row="0" Margin="0,0,0,10"/>

            <Image x:Name="QuestionImage" 
           Source="{Binding CurrentQuestion.ImagePath}" 
           Height="200" Stretch="Uniform" 
           Visibility="{Binding CurrentQuestion.ImagePath, Converter={StaticResource NullOrEmptyToVisibilityConverter}}"
                   Grid.Row="1"/>
        </Grid>

            <StackPanel Grid.Row="2" Margin="0,20" >
            <Button x:Name="Answer1Button" Content="{Binding CurrentQuestion.Answers[0]}" Height="40" Margin="0,5"
                    Click="AnswerButton_Click" Tag="0"/>
            <Button x:Name="Answer2Button" Content="{Binding CurrentQuestion.Answers[1]}" Height="40" Margin="0,5"
                    Click="AnswerButton_Click" Tag="1"/>
            <Button x:Name="Answer3Button" Content="{Binding CurrentQuestion.Answers[2]}" Height="40" Margin="0,5"
                    Click="AnswerButton_Click" Tag="2"/>
            </StackPanel>
        

        <TextBlock Grid.Row="3" Text="{Binding ScoreText}" FontSize="18" FontWeight="Bold" 
                             HorizontalAlignment="Center" VerticalAlignment="Bottom" />

        
        

    </Grid>
</UserControl>
